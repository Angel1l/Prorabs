<Window x:Class="Сафари.Views.ViewForMaterials.BuyMaterialsByUsers"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Сафари.Views.ViewForMaterials" xmlns:materialsmodels="clr-namespace:Сафари.Data.Models.MaterialsModels" d:DataContext="{d:DesignInstance Type=materialsmodels:CountOfBuyingByUsersMaterials}"
        mc:Ignorable="d"
        Title="BuyMaterialsByUsers" Height="300" Width="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Vertical"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="200">
                <TextBlock Text="В якій кількості?"/>
                <TextBox Text="{Binding count}"/>
            </StackPanel>
            <Button Grid.Row="1" Content="Ок" 
                Click="Button_Close_Click"/>

        </Grid>
    </Grid>
</Window>
